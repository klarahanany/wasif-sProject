<!DOCTYPE html>
<html lang="en">
<%- include('partials/header'); -%>

    <head>
        <title>reset password</title>
    </head>
    <script>
        var status = '<%- JSON.stringify(status) %>';

        if (JSON.parse(status) === "verified") {
            if (confirm("Do u want to login?")) {
                window.location.href = "http://localhost:3000/login";
            }
        } else   if (JSON.parse(status) === "error") {
            alert("not the same password try again.")
        }
    </script>
    <!-- </head> -->

    <body>
        <h1>
            <%= email%>
        </h1>
        <form action="" method="post">
            <label for="passsword">Password:</label>
            <input type="password" name="password" id="password" placeholder="Password" /><br />
            <label for="confirm-password">Confirm Password:</label>
            <input type="password" name="confirmpassword" id="confirm-password" placeholder="Confirm Password" /><br />
            <input type="submit" value="RESET" class="btnadd" />
        </form>
        <!-- <%if(status){%>
            <p style="text-align: center;color: green;">
                <%status%>
            </p>
            <%}else if(error){%>
                <p style="text-align: center;color: red;">
                    <%error%>
                </p>
                <%}%> -->


    </body>

</html>